This is the shop page

<ul>
            <% @products.each do |product| %>
             <li>
             <%= link_to product.title, product, class: "btn btn-info btn-small" %>
            </li>
           <% end %>
</ul>
